{
  "name": "fun-with-ml-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.0.0",
    "fun-with-ml-schema": "file:../fun-with-ml-schema",
    "graphql": "^0.13.2",
    "pg": "^7.4.3"
  },
  "devDependencies": {
    "graphql-code-generator": "^0.10.7",
    "prettier": "^1.14.2",
    "typescript": "^3.0.1"
  }
}
